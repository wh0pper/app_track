<!DOCTYPE html>
<html>
  <head>
    <title>Job Tracker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <% if current_user %>
        <h4>Logged in as: <%= current_user.email %> | <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></h4>
      <% else %>
        <h4>Sign-up to get started tracking your job applications</h4>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
